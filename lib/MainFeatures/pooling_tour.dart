import 'package:flutter/material.dart';

class PoolingTour extends StatelessWidget {
  static const poolroute = 'poolingtour';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pooling Tour'),
      ),
      body: Center(
        child: Text('pooling tour'),
      ),
    );
  }
}
