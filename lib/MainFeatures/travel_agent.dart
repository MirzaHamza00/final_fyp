import 'package:flutter/material.dart';

class TravelAgent extends StatelessWidget {
  static const routeName = 'travelAgent';
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('travel Agent'),
        ),
      ),
    );
  }
}
